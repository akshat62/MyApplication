<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Rendered reels: {context.filesDir}/reels/ -->
    <files-path name="reels" path="reels/" />
    <!-- Transient staging (extracted audio, intermediate render steps): cacheDir/staging/ -->
    <cache-path name="staging" path="staging/" />
</paths>
